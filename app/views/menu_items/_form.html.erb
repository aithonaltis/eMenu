<%= form_for(@menu_item) do |f| %>
  <% if @menu_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu_item.errors.count, "error") %> prohibited this menu_item from being saved:</h2>

      <ul>
      <% @menu_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t('menu_item.name') %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label t('menu_item.description') %><br />
    <%= f.text_area :description %>
  </div>
	<div class="field">
		<%= f.label t('menu_item.type')  %><br />
		<%= collection_select(:menu_item, :item_type_id, ItemType.all, :id, :name, {:include_blank => t(:select_blank)})%>
	</div>
  <div class="field">
    <%= f.label t('menu_item.price')  %><br />
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
